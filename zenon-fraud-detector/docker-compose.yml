services:
  db.transaction:
    image: mysql:9.6.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: senha123
      MYSQL_DATABASE: transaction
    ports:
      - "3306:3306"
    volumes:
      - mysql.transaction:/var/lib/mysql

volumes:
  mysql.transaction: